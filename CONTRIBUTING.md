# 参与维护

欢迎通过 [Issues](https://github.com/Mister-Shaw/isg-technology-radar/issues) 提交问题，通过 Pull Request 提交可核对的改动。

## 开发

按 [README](README.md) 完成本地安装和数据库初始化。提交前运行：

```sh
npm test
npm run build
```

涉及交互时，在本机打开页面检查相应栏目、编辑和导出。构建成功不等于已完成页面检查；在 Pull Request 中写清改动目的、验证结果和未验证的部分。接口写入测试只用于可丢弃的本地数据库。

保持依赖锁文件与 `package.json` 一致。提交应用源码和必要迁移，不提交 `node_modules/`、`dist/`、`.wrangler/`、密钥、个人数据或本机运行日志。数据库变更生成新的迁移；已经发布的迁移保留原样，避免破坏其他副本的升级顺序。

## 研究资料

新增记录应提供原始链接、标题、披露日期、项目或事件标识、证据状态与核验说明。没有证据的字段留空，不把采集日期当成事件日期；采集或导入先进入待核状态。

事实、推断和假设应明确区分。解释变化的业务意义，给出有依据的优先行动与下一步核验；不把新闻计数当成需求、采用率或市场份额。保留预算、成交、合同、总投资及区间金额的原始含义，避免跨阶段或多标包重复相加。

修正旧证据时保留可追溯的 ID、原文和原因。不要用旧快照覆盖他人的新编辑。提交公开资料前检查来源权利与个人信息，遵循 [DATA_NOTICE.md](DATA_NOTICE.md)；未经授权，不加入内部文件、付费全文或个人数据库。

每周流程见 [WEEKLY_UPDATE.md](WEEKLY_UPDATE.md)。原维护者的定时任务与发布权限不随克隆转移，新增自动化需自行配置并验证数据截止日、失败处理和重复保护。
