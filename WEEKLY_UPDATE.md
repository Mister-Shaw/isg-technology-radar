# 本地滚动更新与异动监测

开源版本提供来源目录、公开页面读取与文件导入、分类、金额去重、趋势和异动计算代码。默认不安装定时任务，不包含原站的 Codex 任务、商业数据授权、生产数据库或通知权限。

## 一次更新

1. **先备份**：停止服务后备份 `.wrangler/state`，在页面导出当前研究 JSON。保留人工笔记、ID 和版本历史，不用旧文件覆盖当前修改。
2. **确定窗口**：以北京时间最近一个完整周日为截止，补采上次成功截止后的资料，回查此前14天，月初回补上月。日期缺失不猜测；部分月单列。
3. **读取来源**：在“来源与采集”使用登记入口或导入有权取得的资料。可复用 [tools/collectors](tools/collectors/README.md) 中的公开采集器。网页结构和访问条件可能变化；输出是候选结果，需要审核，运行成功不代表全量覆盖。
4. **维持可比口径**：月度研究按事件整理；市场风向保持固定媒体同期全部去重新闻为分母，技术、芯片和地域条件只筛分子。新增媒体先单列或回采全窗口，不把关键词检索结果拼进分母。父类取底层记录并集，不能相加子类。
5. **核查原文与金额**：保留买方、项目编号、标包、事件日、披露日、阶段、金额性质和链接。预算、候选、合同全期、投资不当成交额；转载去重，不同阶段保留。金额图使用 `combinedAmountRows` 取已知项目/标包最新披露，区间保留上下限。
6. **同步资料**：研究底稿在 `data/research-2026.json`，固定样本在 `public/market-panel.json`，来源覆盖在 `public/sample-sources.json`，入口在 `data/sources.js`。网页编辑另存于本地数据库覆盖层；替换底稿前先合并当前编辑。更新真实窗口、采集日期、成功/失败和覆盖范围。
7. **重算与检查**：`node scripts/refresh-taxonomy.mjs YYYY-MM-DD` 重算当前全窗口分类，不代表新增采集。运行 `npm test`、数据守恒检查和 `npm run build`。历史测试针对附带基线；新增周期需保留历史回归并增加新窗口检查。

## 计算异动草稿

首次按快照真实截止日建立基线（示例为附带快照日期）：

```sh
node scripts/weekly-monitor.mjs --init --end 2026-09-20
```

后续先审核新数据，再传入真实截止日和当前网页备份 JSON：

```sh
node scripts/weekly-monitor.mjs --end YYYY-MM-DD --research path/to/current-backup.json
```

输出位于 `weekly-runs/monitor-YYYY-MM-DD.json`；成功状态位于 `weekly-runs/last-success.json`，两者已从 Git 排除。基线已存在时 `--init` 会退出，不覆盖旧基线。

此脚本只计算，不抓取新闻、不发通知、不发布，也不在普通运行时推进成功水位。维护者审核覆盖并确认保存成功后，将输出的 `next_state` 写为 `last-success.json`，按实际通知结果维护 `notified_ids`。`complete=false` 时保留旧成功水位，优先补齐来源。

## 规则与调度

规则在 `data/weekly-monitor.json`。完整7天对前28天：新闻占比绝对变化至少5‰、相对变化至少50%，命中规模、全部新闻分母和至少2家同向媒体均须满足条件。成交/合同、预算、投资分别使用各自阈值；海光、鲲鹏重点路线及取消、冲突单列。

阈值不是统计显著性结论。先检查覆盖和发稿结构变化，再解释市场信号；提及不等于采用，历史补录不当本周新增需求，不为凑数量放宽日期与去重标准。

如需每周执行，由维护者在自己的系统任务计划或调度器配置采集、审核和通知流程。只有实际设置成功后才能把 `enabled` 改为 `true`；改这个字段不会创建定时任务。

原站详细代理工作流保留在 [历史参考](docs/reference/hosted-weekly-workflow.md)，其中 Sites 发布和 Codex 通知不是本地默认功能。原始面板构建脚本说明见 [历史重建](docs/HISTORICAL_REBUILD.md)。
