# 自动更新与每周研究

## 一键运行

准备 Node.js 22.13+、Python 3.10+，先完成 `npm ci`。在仓库目录执行：

```sh
npm run update -- --dry-run
npm run update
```

`--dry-run` 联网采集并生成检查报告，不写回网站数据和成功水位。普通运行按“来源×月份”独立检查并写回，通过的月份不再被其他来源或月份的失败阻塞。本机不支持 `python` 命令时，可使用环境变量 `PYTHON` 或指定可执行文件，例如：

```sh
npm run update -- --python /usr/bin/python3
```

Windows 路径含空格时加引号，例如 `--python "C:\Program Files\Python313\python.exe"`。脚本自身不需要数据库初始化，不使用线上凭据。

## 自动做什么

1. 每轮从 `public/market-panel.json` 的 `window.start` 全量重新采集至北京时间最近一个已经结束的周日。附带快照起点为2026年1月1日；不会按上次成功日缩短范围，跨年也不重置起点，因此首次部署、漏跑和较早月份的本地数据缺口均可通过全量回查补齐。也可传入 `--end YYYY-MM-DD`，必须是已结束的周日且不早于当前数据截止。
2. 重新读取 DOIT、C114、至顶、电脑商情四个固定媒体的公开归档。保留全部主题，以同源规范化标题去重，技术、芯片、地域只筛分子。父类仍取记录并集。新闻占比是关注度，不是采用率。
3. 检查 robots、目录遍历、日期、ID 和来源完整性。每个来源的 `sources[].months` 提供分月覆盖证明，再逐来源、逐月份比较本次抓取量与原库同期数量；历史月抓取量不足一半、完整自然月零返回或覆盖未经证明时，仅暂缓该来源该月。通过的月份正常写入，失败月份完整保留旧记录、去重状态与实际成功截止；无旧数据则标记 `missing`，不填零。跨月日期更正涉及失败月份时，相关月份一并暂缓；跨月重复标题暂以失败月份保留的主记录为准，补齐后恢复按日期去重。即使空库首次部署，也检查每个完整月；未完成月可以暂时零返回。完整仅表示当前可访问归档遍历完成，不能证明全网或历史删除内容全量覆盖。
4. 同步 `public/market-panel.json`、`public/sample-sources.json`、`public/taxonomy-review.json` 和 `data/weekly-monitor.json`，自动计算尝试窗口、月份列表与月数，市场风向热力图自动扩列。每个来源月份显示 `complete`、`stale`（沿用旧数据）或 `missing`、实际覆盖截止、最近尝试与异常原因。固定四媒体口径不变，覆盖不齐月份的跨来源比例留空，保留数量作诊断；成功来源及完整月份仍可独立查看。未补采的官方/采购来源不会自动变成完整；新增标题未经行为复核时，对应月份的采购/部署比例暂不显示。
5. 生成异动与研究候选。只有所有来源月份通过（`summary.complete`）且完成写回，才推进 `weekly-runs/last-success.json` 和 `last_completed_cutoff`；部分更新只推进尝试记录，保留上次完整成功水位。周信号的新闻量或命中数不足只暂停定量异动，不阻止成功采集数据入库；计算周比例仍须固定四媒体覆盖本周及前4周，覆盖不足时比例、基线与变化值留空。旧记录本轮未在来源重新出现时保留，并在 `source_month_checks` 的 `retained_not_seen_ids` 中列出，供排查历史删除、改版或漏采；不覆盖研究底稿或 `.wrangler/state` 中的笔记和修订。

更新后，开发服务刷新页面；构建运行版本需重新 `npm run build` 并重启。公开演示站、远端 GitHub 和其他人的本地副本不会被这个命令更新。

## 输出与失败处理

每批结果位于 `weekly-runs/<时间和批次ID>/`：

| 文件 | 内容 |
| --- | --- |
| `report.json` | 全量窗口、本轮抓取数、累计去重数、新增/变化数、逐源逐月核对及是否已写回 |
| `monitor.json` | 当前完整周对前4周的指标、异动、质量提示 |
| `research-candidates.json` | 新增或变更的技术标题线索；正文、地域、阶段、金额均需核验 |
| `preview-news.json` | 重放文件中超出完整周截止的预览资料，不计入当期 |
| `deferred-news.json` | 失败或依赖未解决的月份中，本次读到但暂未采用的记录 |
| `backup/`、`staged/` | 普通运行写回前的备份与本批数据，包含完整或部分更新；预演不生成 |
| `failure.json` | 失败原因；采集失败的逐源详情位于下述原始输出目录 |

采集审计和原始响应保存在 `tools/collectors/output/<批次ID>/`。以上本机资料已被 Git 忽略，不应公开提交采集全文或个人研究备份。

主入口的退出码：

| 退出码 | 含义 |
| --- | --- |
| `0` | 全部来源月份完成检查；普通运行已写回，预演仅报告 |
| `2` | 部分成功；普通运行已写回通过的月份，失败月份保留旧数据或缺口；预演仅报告 |
| `1` | 数据结构、执行或写回失败；见 `failure.json`，本批写回失败会回滚 |

底层 Python 采集器仍只返回 `0` 或 `1`。采集器退出 `1` 但已保存结构完整的审计结果时，主入口会继续逐月检查并采用通过的月份；未取得可用审计、非法日期或冲突 ID 等结构问题仍终止整批。全量请求每次重新联网，不复用历史响应缓存。DOIT和至顶持续翻页至整页日期早于研究起点；重复页、空页、超时或请求错误明确失败。C114逐日核对完整日期表；CBI保留每栏目1,000页上限，触顶明确失败。总体限时60分钟；来源改版或发稿骤降也可能需要人工排查。脚本不会为通过检查而填充数据或放宽阈值。

写回使用备份、单文件原子替换和事务日志。写入失败即恢复旧数据；进程意外退出后，下一次运行先恢复未完成事务。发现 `weekly-runs/update.lock` 时不会并发运行；确认旧更新进程已停止后才移除这个空目录并重试，保留 `update-transaction.json` 和批次备份用于恢复。更新过程中不要手动改动上述输出文件。

已保存有效采集审计、但合并阶段中断时可以重放原始批次，避免重复联网；仍执行同样的日期与质量检查，不会跳过检查：

```sh
npm run update -- --input tools/collectors/output/BATCH_ID/media-refresh.json --end YYYY-MM-DD
```

批次的请求范围必须覆盖研究起始日至指定完整周截止；其中失败月份可以保留缺口，但过去的14天增量请求批次会被拒绝。预演、重跑都不发送通知；`notified_ids` 只应由实际通知处理程序在成功发送后维护。

## 每周定时执行

克隆本仓库不会安装任务，也不继承原站 Codex 自动化。可在自己的 Windows 任务计划程序或现有 Linux/macOS 调度器中每周一运行同一个入口：

- 程序：本机 `node` 的绝对路径。
- 参数：`"仓库绝对路径/scripts/update-data.mjs" --python "Python可执行文件绝对路径"`。
- 工作目录：仓库根目录；建议每周一北京时间09:00，避免任务重叠。
- 保存标准输出和错误输出；`2` 表示已部分写回、仍需补齐，`1` 表示执行失败；两者都需要检查，不能统称“全部未更新”。通知程序读取 `monitor.json`，先区分覆盖故障与市场异动，不把缺失数据解释为需求下降。

只有任务在自己的机器上配置并测试成功后，才应更改 `data/weekly-monitor.json` 的 `enabled` 和 `schedule_label`。单改字段不会创建任务；本脚本也不包含邮件、聊天通知或发布权限。

## 月度研究与金额

自动标题线索独立保存，未自动作为项目事件入库。核实原文、买方、项目编号、标包、事件日、披露日、阶段及地域后，通过“来源与采集”导入，每批最多50条。不要直接把整个候选文件当成已核研究包；`categories` 中的海光/鲲鹏观察路线需转换为导入表里的芯片路线，HTTP旧链接需先核对可用的HTTPS原文。

官方采购、公众号、未授权商业服务仍由维护者查询或导入有权取得的资料。保留证据状态；预算、成交、合同、总投资和区间金额沿用原始性质，按已知项目/标包最新披露去重。历史补录不当本周市场增长。金额未披露留空，不能用标题推算。

网页编辑另存于本地数据库覆盖层。需要把当前本机研究编辑纳入异动计算时，先从网页导出 JSON，再执行：

```sh
npm run update -- --research path/to/current-backup.json
```

该参数只用于研究分类索引与异动计算，不把备份写回研究底稿或数据库。运行前后均应保护本机备份；分享生成的异动资料前检查是否含私人内容。

异动阈值在 `data/weekly-monitor.json`：完整7天对前28天，新闻占比绝对变化至少5‰、相对变化至少50%，并满足命中数、全部新闻分母和至少两家同向媒体条件；采购金额按其性质分别设阈值。这些是监测规则，不是统计显著性结论。

## 验证

```sh
npm test
python tools/collectors/check_cli.py
npm run build
```

测试包含全窗口起点、跨年不缩短范围、旧月份缺口修复、空新闻面板重建、失败月份隔离、旧记录与实际截止保留、重试恢复、重复运行、固定全部新闻分母、覆盖不齐时比例留空、未复核行为、写入失败回滚和中断恢复；另验证翻页超过旧30/150页限制、日期边界和单日失败只影响所属月份。联网采集结果以各批次 report.json 为准，成功记录不代表未来来源可用性。

2026-09-23 已联网全量验证2026-01-01至2026-09-20：四媒体原始记录21,293条，去重后21,146条，36个来源月份组合均完成核对。至顶遍历503页、C114核对263个日期；本轮与附带快照一致，无新增。报告将本轮抓取数与累计去重数分别列出。

同日用新版本再次联网预演，36个来源月份全部通过，数量与上述结果一致。在隔离副本模拟一个来源的8月失败：其余35个来源月份成功写回，失败月份记录及分母保持不变、完整成功水位不推进；重跑无重复，恢复后36个来源月份全部通过并推进水位。测试用的模拟记录没有写入仓库公开数据。

原站代理研究流程见 [历史参考](docs/reference/hosted-weekly-workflow.md)；早期全窗口脚本见 [历史重建](docs/HISTORICAL_REBUILD.md)。
