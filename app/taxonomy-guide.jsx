import {GROUPS,CHILDREN,TAXONOMY_NOTE,XPU_NOTE} from '@/lib/hierarchy';
export default function TaxonomyGuide(){return <section className="card"><h2>五个父类与十四个观察子项</h2><p>{TAXONOMY_NOTE}</p><div className="table-scroll"><table><thead><tr><th>父类</th><th>子项</th></tr></thead><tbody>{GROUPS.map(g=><tr key={g.id}><th>{g.name}</th><td>{g.children.map(id=>CHILDREN[id]).join('、')}</td></tr>)}</tbody></table></div><p className="footnote">{XPU_NOTE} 海光CPU、DCU与鲲鹏CPU仍保留独立路线标签。超节点描述系统，算力场描述资源池/调度与一体机部署，Token工厂描述运营形态，允许同一项目交叉归类。</p><p className="footnote">2026-09-23启用业务语义分组，统一重算当前窗口。新闻仅用标题，月度研究用记录中的标题、事实摘要及芯片标签。原始分类、记录ID、金额和用户笔记保留；海光、鲲鹏及xPU已完成1—9月定向补采，记录保留核验状态。新增主题采购适用分母及媒体采购/部署标题行为复核尚未完成；这套分组覆盖当前关注清单，不代表全市场技术目录完备。</p><a href="/taxonomy-review.json" download>下载分类规则与历史重算清单</a></section>;}
